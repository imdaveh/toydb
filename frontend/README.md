Frontend setup

1. Install dependencies:
   cd frontend
   npm install
2. Start dev server:
   npm run dev

Notes
- Vite dev server runs on http://localhost:5173 by default.
- The frontend uses the VITE_API_BASE env var to call the backend.
- Tailwind is configured; if styles look missing ensure you installed devDependencies.

